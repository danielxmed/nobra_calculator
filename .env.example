# Rate Limiting Configuration

# Redis connection URL (format: redis://host:port or just host:port)
REDIS_URL=YourRedisEndpointHere

# Redis password
REDIS_PASSWORD=YourRedisPasswordHere

# Rate limit: requests per second per IP (REQUIRED - no default)
REQ_PER_SEC=int

# Whitelist: IPs exempt from rate limiting
# Format: comma-separated list or JSON array
# Examples:
# WHITE_LIST=192.168.1.1,10.0.0.1,172.16.0.1
# WHITE_LIST=["192.168.1.1", "10.0.0.1", "172.16.0.1"]
WHITE_LIST=list

# Optional: Port for the API (default: 8000)
PORT=8000